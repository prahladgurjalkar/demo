CREATE TABLE `azayditc_database`.`contact` (
  `name` VARCHAR(70) NOT NULL,
  `email` VARCHAR(45) NOT NULL,
  `phone number` VARCHAR(45) NOT NULL,
  `message` VARCHAR(45) NOT NULL,
  PRIMARY KEY (`email`));
